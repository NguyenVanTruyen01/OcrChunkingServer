# OCR-Chunking Service

## Prerequisites
- **Docker** and **Docker Compose** installed
- A `.env` file with your Azure credentials  
  (see [`docker/.env.example`](docker/.env.example) for required variables)

---

## Quick Start

### 1️⃣ Run the API
Start the containers in the background:
```bash
make run